"use strict"; //Treats the JS code as the newer version

// alert(3+3) //we are using node.js, not browser

// console.log(3+3);

const name = "Akash";
let age = 18;
let isLoggedin = false;
let State = null;

console.table([name, age, isLoggedin, State]);
// console.table([typeof("Akash")])

console.log(typeof undefined);
console.log(typeof null);

//
/**
 * Important:
 * 1. Code Redability should be High!
 * 2. Clean Code

 * Datatypes:
    Number => 2 to the power 53
    string = ""
    boolean = true/false
    bigint
    null -> Stand-Alone Value 
    undefined =>
    Symbol => Unique
*/
